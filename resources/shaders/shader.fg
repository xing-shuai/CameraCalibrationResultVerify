#version 330 core
out vec4 FragColor;

in vec2 TexCoord;
in vec3 pointPos;

void main()
{
     FragColor = vec4(0.5,0.5,0.5,0);
     if(pointPos.x==0)
	    FragColor = vec4(1,0,0,0);
	 if(pointPos.y==0)
	    FragColor = vec4(0,1,0,0);
}